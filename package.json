{
    "name": "select",
    "version": "0.9.0",
    "description": "模拟 select 的组件。",
    "tag": "widget",
    "dependencies": {
        "$": "$",
        "overlay": "0.9.10",
        "templatable": "widget/1.0.2/templatable"
    },
    "devDependencies": {
    },
    "output": {
        "select.js": "."
    },
    "tests": [
        "select"
    ]
}
